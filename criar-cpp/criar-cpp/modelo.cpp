#include <iostream>
#include <string>
#include <vector>

#define DEBUG 1
#define debug(valor) if(DEBUG) std::cout << "\t<" << valor << ">" << std::endl
#define para_cada(instancia, it) for(it = instancia.begin(); it != instancia.end(); ++it)

using namespace std;

typedef std::vector<int> vi;
typedef std::vector<std::string> vs;

int main (int argc,char **argv) {
    debug ("Debug ativado");

    // Código

    return 0;
}
